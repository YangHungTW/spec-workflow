# Review: 20260418-review-capability

Date: 2026-04-18 14:50
Axis: all
Mode: one-shot (post-archive), feature-diff = `c82ad66^1..c82ad66^2` (5006 lines, 40 files)

## Consolidated verdict

Aggregate: **NITS** (exit code 0)
Findings: 0 must, 11 should, 2 advisory (13 total across 3 axes)

No BLOCK findings. All 3 reviewers returned NITS. The feature ships coherently; findings are quality-improvement candidates, not release blockers.

---

## Security

### Summary
Reviewed the 5006-line feature diff for security-axis findings only. The feature lands: 3 reviewer agent files, 3 rubric files, `/specflow:review` command, `--skip-inline-review` flag + 7-step inline review injection in implement.md, a SessionStart hook SKIP_SUBDIRS guard, and 5 new test scripts.

**Positives**:
- All 5 new test scripts (t34–t38) apply `sandbox-home-in-tests` discipline correctly: `mktemp -d`, trap EXIT, exported HOME, case-based preflight.
- Report-file write documented as write-temp-then-rename (atomic swap), with 3-tier no-clobber fallback (minute → second → pid suffix).
- Parser loops use `while IFS= read -r line; do case ...` — no string-to-shell injection surface.
- Aggregator is pure-classifier (no mutation), matching `classify-before-mutate`.
- No hardcoded secrets, no eval, no `yaml.load`, no string-concat'd shell commands.
- SessionStart SKIP_SUBDIRS guard uses safe whole-word `case " $SKIP_SUBDIRS " in *" $subdir "*)` match.

```
## Reviewer verdict
axis: security
verdict: NITS
findings:
  - severity: should
    file: .claude/commands/specflow/review.md
    line: 7-11
    issue: `<slug>` user input flows into filesystem paths and `git diff main...<slug>` / `git log ... <slug>` without an explicit allowlist check documented at the argument-parse boundary (Step 1). A slug starting with `-` could be parsed as a git flag; `..`-containing slugs rely solely on the feature-dir existence check as a boundary. Other specflow commands share this gap, so this is a pattern-level nit rather than a regression.
    suggested-fix: Add a Step 1 bullet: "Validate `<slug>` against `^[a-z0-9][a-z0-9-]*$` (kebab-case per README.md frontmatter-schema); reject with exit 2 otherwise." This bounds both the filesystem and git-ref surfaces at a single boundary.
```

---

## Performance

### Summary
Diff is mostly documentation; executable shell surfaces are tiny. Only 7 new lines in `session-start.sh` (O(1) per subdir). Test scripts iterate over 3 axes/rubric files — small constant. No `must` performance violations.

```
## Reviewer verdict
axis: performance
verdict: NITS
findings:
  - severity: should
    file: test/t35_reviewer_rubric_schema.sh
    line: ~106
    issue: Same rubric file is re-read 6+ times per iteration (one `awk` for frontmatter, four separate `grep -n` calls for headings, one `awk | grep -cE` for checklist) inside the loop over 3 rubrics — rubric #5 no-re-read pattern.
    suggested-fix: Fold all heading-line detection + frontmatter extraction + checklist counting into a single `awk` pass per file that emits all needed values; eliminates 4–5 redundant file opens per iteration.
  - severity: advisory
    file: test/t34_reviewer_verdict_contract.sh
    line: ~3917
    issue: `check_agent` spawns 7+ `grep` subprocesses per agent file plus a nested 5-key `for` loop with another grep per key (~12 forks × 3 agents); rubric #6 fork/exec density in hot paths.
    suggested-fix: Read each agent file once into a variable (`content=$(cat "$agent_file")`) and run greps against `printf '%s\n' "$content" | grep ...`, or batch schema-key checks into a single awk pass. Below the tight-loop bar (3 agents), but the pattern will compound if the axis count grows.
```

---

## Style

### Summary
Cross-file naming-convention drift between the 3 new sibling reviewer agents (team-memory paths don't match); `set -u` omitted `-o pipefail` in some new tests vs the directory-dominant `-u -o pipefail`; several WHAT-not-WHY comments. No must-severity issues — all advisory/should housekeeping.

```
## Reviewer verdict
axis: style
verdict: NITS
findings:
  - severity: should
    file: .claude/agents/specflow/reviewer-security.md
    line: 12
    issue: Team-memory path `~/.claude/team-memory/reviewer-security/` drifts from neighbour files — `reviewer-style.md` and `reviewer-performance.md` both use `~/.claude/team-memory/reviewer/`. Sibling-file convention mismatch.
    suggested-fix: Pick one path convention across all three reviewer agents and align (pair-per-axis `reviewer-<axis>/` or shared `reviewer/`).
  - severity: should
    file: .claude/agents/specflow/reviewer-style.md
    line: 12
    issue: Team-memory invocation block prose is terser than siblings; `reviewer-security.md` and `reviewer-performance.md` use a numbered `ls` checklist.
    suggested-fix: Align the three sibling files on one Team-memory-block shape.
  - severity: should
    file: test/t34_reviewer_verdict_contract.sh
    line: 7
    issue: `set -u` omits `-o pipefail` — majority of scripts in `test/` (including t35, t36) use `set -u -o pipefail`; match neighbour strictness (rubric entry 7).
    suggested-fix: Change `set -u` to `set -u -o pipefail`.
  - severity: should
    file: test/t37_review_oneshot.sh
    line: 8
    issue: Same `set -u` without `-o pipefail` departs from directory convention.
    suggested-fix: Align with neighbours.
  - severity: should
    file: test/t38_hook_skips_reviewer.sh
    line: 8
    issue: Same `set -u` without `-o pipefail` departs from directory convention.
    suggested-fix: Align with neighbours.
  - severity: should
    file: test/t26_no_new_command.sh
    line: 57
    issue: Comment "# Count files only (not directories) in the commands dir" narrates WHAT; drop or explain WHY directories are excluded.
    suggested-fix: Drop the WHAT comment or rewrite to state the intent.
  - severity: advisory
    file: test/t35_reviewer_rubric_schema.sh
    line: 106
    issue: Comment "# Extract line numbers of each required heading" narrates WHAT the grep does — code is self-evident.
    suggested-fix: Drop or rewrite to explain WHY.
  - severity: advisory
    file: .claude/commands/specflow/implement.md
    line: 96
    issue: Heredoc-style pseudocode block mixes 3-space indent inside an otherwise 2-space pseudocode block; copy-paste drift risk.
    suggested-fix: Normalize indentation to 2 spaces throughout the pseudocode block.
```

---

## Notes on this review

- **Invoked post-archive**: feature is already merged to main. No mergeable changes from this report — all findings are cleanup candidates for a follow-up housekeeping commit or the next feature.
- **Bootstrap workaround**: native subagent registration for `reviewer-security` / `reviewer-performance` / `reviewer-style` was cached off in this session (post-merge). The review was run with `general-purpose` agent acting as each reviewer, using the same rubric + identity prompts. Output contract (D1 footer) was honored. A fresh Claude Code session will pick up the native reviewer subagents.
- **Meta-demo**: this is the first real exercise of `/specflow:review`, run against the feature that shipped it. The reviewers reviewing themselves. Findings confirm the reviewer infrastructure works end-to-end.
