# Plan — chore: STATUS render shape for matrix-skipped stages

- **Feature**: `20260426-chore-status-template-skip-stages`
- **Stage**: plan
- **Author**: orchestrator (hand-written; chore-tiny short-circuit — see §1.3)
- **Date**: 2026-04-26
- **Tier**: tiny
- **Work-type**: chore

PRD: `03-prd.md` (chore checklist).

## 1. Approach

### 1.1 Scope

Edit `.claude/commands/scaff/next.md` so the chore × tiny matrix-skip pseudocode (and the orthogonal `has-ui: false` design-skip path) renders matrix-skipped stages as `[~] <stage> ... (skipped — chore × tiny matrix)` (or `(skipped — has-ui: false)`) instead of flipping the leading checkbox to `[x]`. Then update `.claude/team-memory/qa-analyst/chore-tiny-status-checkbox-vs-notes-asymmetry.md` to either reflect the new convention (rewrite §How-to-apply step 3 to name `[~]` as the new shape and acknowledge the asymmetry is now resolved going forward) or retire the file outright. No production-code changes; no `_template/STATUS.md` change; no `bin/scaff-stage-matrix` change.

### 1.2 Why one task

Both files form a single coherent edit: the orchestrator command file is the canonical source of the new convention, and the analyst memory file documents how analysts treat the convention. Splitting the doc-update from the convention-source would create a window where the memory file and the orchestrator file disagree, which is the very ambiguity this chore is closing. Folded into T1.

### 1.3 Chore-tiny short-circuit

The stage matrix for `chore × tiny` reports: `design = skipped`, `tech = skipped`, `plan = optional`. The `/scaff:plan` command hard-requires `04-tech.md` (its step 1: "Require 03-prd.md AND 04-tech.md exist"), but tech is matrix-skipped on this tier. Rather than (a) error out at /scaff:plan dispatch or (b) have TPM short-circuit on missing prereq, the orchestrator hand-writes this minimal plan from the chore PRD's checklist. This file exists primarily to satisfy `/scaff:implement`'s contract (it requires `05-plan.md` with at least one `- [ ]` line). See `.claude/team-memory/tpm/chore-tiny-plan-short-circuit-plumbing-gap.md` for the plumbing-fix options (A or B); this file should not be regenerated by TPM — it is intentionally hand-written until the plumbing lands.

Note: this chore is itself initialised pre-fix and uses the legacy `[x]` render shape per PRD §Out-of-scope (forward-only application). The next chore × tiny feature shipped after this lands will be the first to use `[~]`.

### 1.4 Wave shape

Single wave, single task. No inline review (`R16` default for `tier=tiny`). No worktree needed for one task — `--serial` mode. Default validate run after implement completes.

## 2. Tasks

## T1 — Update next.md skip-render and qa-analyst memory; preserve right-hand annotations
- **Milestone**: M1
- **Requirements**: chore PRD §Checklist items C1–C4 (folded)
- **Decisions**: chore PRD §Decisions (a)-(e); render shape `[~] <stage> (<original right-side annotation>) (skipped — chore × tiny matrix)` or `(skipped — has-ui: false)` per the relevant skip arm; flip mechanic preserves the leading checkbox swap and appends a suffix only — does NOT replace the entire line text or strip role attribution; touched files = `.claude/commands/scaff/next.md` + `.claude/team-memory/qa-analyst/chore-tiny-status-checkbox-vs-notes-asymmetry.md`; `bin/scaff-stage-matrix` and `_template/STATUS.md` are NOT touched
- **Scope**:
  - `.claude/commands/scaff/next.md` — locate the matrix-driven skip pseudocode (Step 4, the `case "$status" in skipped)` arm — currently described around lines 54–69 in the file). Update the comment block AND the inline pseudocode to specify the new render shape (`- [~] <stage> ... (skipped — chore × tiny matrix)`) instead of `[x]`. Keep the STATUS Notes line format unchanged. Apply the same change to the `has-ui: false` design-skip path (Step 4 penultimate bullet, around the same region) using `(skipped — has-ui: false)` as the suffix.
  - `.claude/team-memory/qa-analyst/chore-tiny-status-checkbox-vs-notes-asymmetry.md` — Developer chooses between (a) updating §How-to-apply step 3 to name `[~]` as the new shape and noting the convention is now self-describing, or (b) retiring the file (removing it) since the fix forward eliminates the ambiguity. Recommendation: option (a), because already-archived chore-tiny features retain the `[x]` shape and future analysts may still encounter that legacy form. Update the memory's `updated:` frontmatter date to 2026-04-26 if option (a); otherwise commit with a clean removal.
- **Deliverables**:
  - `.claude/commands/scaff/next.md` (edit; doc-block change + pseudocode change in two skip arms; estimated ~10–15 line diff)
  - `.claude/team-memory/qa-analyst/chore-tiny-status-checkbox-vs-notes-asymmetry.md` (update or remove)
- **Verify**:
  - C1: `grep -F '[~]' .claude/commands/scaff/next.md` returns at least one match in the matrix-skip pseudocode block.
  - C2: `grep -F 'skipped — has-ui: false' .claude/commands/scaff/next.md` returns the doc-block describing the new shape.
  - C3 (forward-only): not directly testable in this chore; documented as a forward verification that runs on the next chore × tiny feature shipped after this lands.
  - C4: `grep -F '[~]' .claude/team-memory/qa-analyst/chore-tiny-status-checkbox-vs-notes-asymmetry.md` matches OR `[ ! -f .claude/team-memory/qa-analyst/chore-tiny-status-checkbox-vs-notes-asymmetry.md ]`.
  - Adjacent regression: `bash test/t114_seed_settings_json.sh` exits 0 (smoke check that the next.md edit didn't accidentally break any test runner that reads from .claude/).
- **Depends on**: —
- **Parallel-safe-with**: —
- [ ]

## 3. Risks

- **next.md formatting drift** — `.claude/commands/scaff/next.md` is loaded by the orchestrator as instructions; any malformed markdown or breakage of the pseudocode bash block could affect orchestrator behaviour on the next chore × tiny intake. Mitigation: keep the edit narrowly scoped to the skip-arm comment and pseudocode; do not refactor surrounding structure; verify by reading the file after editing and confirming the surrounding flow is intact.
- **Memory file consistency** — if Developer chooses retirement (option b), they must also remove the index entry at `.claude/team-memory/qa-analyst/index.md` for the retired memory; if they choose update (option a), the index hook line should be updated to reflect the new disposition. Mitigation: pick option (a) by default; index.md hook stays approximately the same; only the file body changes.

## 4. Open questions

None. Memory disposition (update vs retire) is at Developer's discretion within T1; recommendation pinned to option (a) update in §Scope.
