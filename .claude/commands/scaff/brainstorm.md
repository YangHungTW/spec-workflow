---
description: RETIRED — see /specflow:prd. Usage: /specflow:brainstorm <slug>
---

This command was retired in feature `20260420-tier-model`.
Folded into `/specflow:prd`; see PRD R4 mapping.

Action:
- Run `/specflow:prd <slug>` instead.
- The artefact `01-brainstorm.md` is no longer authored; fold exploration into
  the `## Exploration` section of `03-prd.md` per the new schema.

No STATUS mutation occurs. Exits non-zero.
