# .claude/carryover-allowlist.txt
# =============================================================================
# Paths allowed to retain legacy "specflow" / "spec-workflow" string references
# despite the specflow→scaff / spec-workflow→specaffold / .spec-workflow/→
# .specaffold/ rename. Each line is an fnmatch-style glob pattern consumed by
# test/t_grep_allowlist.sh. Blank lines and # comments ignored.
# =============================================================================

.git/**
.specaffold/archive/**
.claude/carryover-allowlist.txt
docs/rename-migration.md
# test/t_grep_allowlist.sh — the grep script contains specflow/spec-workflow as pattern strings in its own source code
test/t_grep_allowlist.sh
# flow-monitor/README.md — R10 upgrade-notes line documents legacy .spec-workflow/.flow-monitor/ audit-log path per D4 lazy-migration
flow-monitor/README.md
# flow-monitor/dist/** — built JS artifacts (gitignored; present on local disk between npm run build and clean). Grep script scans disk, not git tracking.
flow-monitor/dist/**
# .claude/team-memory/** — retrospective memory files from past rename features naturally reference legacy brand as historical examples
.claude/team-memory/**
